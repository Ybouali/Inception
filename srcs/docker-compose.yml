version: '3'
services:
  nginx:
    build: requirements/nginx
    container_name: nginx
    depends_on:
      - wordpress
    ports:
      - "443:443"
    volumes:
      - /Desktop/ybouali/data
  wordpress:
    build: requirements/wordpress
    container_name: wordpress
    ports:
      - "9000:9000"
    volumes:
      - /Desktop/ybouali/data
    depends_on:
      - mariadb
  mariadb:
    build: requirements/mariadb
    container_name: mariadb
    ports:
      - "3306:3306"